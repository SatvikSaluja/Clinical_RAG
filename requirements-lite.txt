# Slim dependency set for memory-constrained deployments (no PyTorch/
# transformers/sentence-transformers) - see README "Deploying on limited
# RAM". Requires EMBEDDING_PROVIDER=remote, RERANKER_PROVIDER=llm, and
# USE_LOCAL_NLI_VERIFIER=false so the app never imports the heavy local-ML
# code paths. For full local/eval use with the biomedical PyTorch models,
# use requirements.txt instead.

fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
python-multipart==0.0.9

rank-bm25==0.2.2
faiss-cpu==1.8.0.post1
numpy==1.26.4

openai==1.51.0
requests==2.32.3
