# --- Backend web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
python-multipart==0.0.9

# --- Retrieval ---
rank-bm25==0.2.2
faiss-cpu==1.8.0.post1
sentence-transformers==3.1.1

# --- NLP / models ---
# NOTE: torch is installed separately from the CPU-only wheel index in the
# install instructions (README) to avoid pulling multi-GB CUDA dependencies
# on machines without a GPU. If already installed, this line is a no-op.
transformers==4.44.2
torch==2.4.1
accelerate==0.34.2
scikit-learn==1.5.2
numpy==1.26.4

# --- LLM provider (OpenAI-compatible) ---
openai==1.51.0

# --- Ingestion (PubMed) ---
requests==2.32.3

# --- Evaluation / misc ---
tqdm==4.66.5
tabulate==0.9.0

# --- Testing ---
pytest==8.3.3
httpx==0.27.2
